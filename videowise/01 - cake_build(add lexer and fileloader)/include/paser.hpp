//#include "token.hpp"

// using CompileToken = std::vector<compileScope::TokenType, std::string>;
// using LinkerToken = std::vector<linkerScope::TokenType, std::string>;
// using OutputToken = std::vector<outputScope::TokenType, std::string>;

/*namespace Candy
{
    class Parser
    {
    public:
        Parser(Tokens tokens);

        CompileToken GetCompileToken() { return m_CompileToken; }
        LinkerToken GetLinkerToken() { return m_LinkerToken; }
        OutputToken GetOutputToken() { return m_OutputToken; }

    private:
        CompileToken m_CompileToken;
        LinkerToken m_LinkerToken;
        OutputToken m_OutputToken;
    };
};
*/